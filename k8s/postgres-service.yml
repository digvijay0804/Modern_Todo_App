apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: todo-app
spec:
  selector:
    app: postgres
  type: ClusterIP
  ports:
  - name: myjob
    protocol: TCP
    port: 5432
    targetPort: 5432